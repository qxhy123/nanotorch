# 第二章（d）：向量矩阵微积分与反向传播

向量矩阵微积分是深度学习的核心数学工具。神经网络的参数通常是矩阵形式，理解如何对矩阵求导对于实现反向传播至关重要。本章将系统介绍矩阵导数公式和反向传播算法的实现。

---

## 目录

1. [向量导数基础](#向量导数基础)
2. [雅可比矩阵](#雅可比矩阵)
3. [矩阵导数公式](#矩阵导数公式)
4. [深度学习中的梯度计算](#深度学习中的梯度计算)
5. [反向传播算法](#反向传播算法)
6. [梯度数值验证](#梯度数值验证)
7. [小结](#小结)

---

## 向量导数基础

### 布局约定

矩阵导数有两种常见的布局约定：

| 布局 | $\frac{\partial y}{\partial \mathbf{x}}$ 的形状 |
|------|-----------------------------------------------|
| 分子布局 | $1 \times n$（行向量） |
| 分母布局 | $n \times 1$（列向量，**我们使用这种**） |

### 标量对向量求导

设 $f: \mathbb{R}^n \to \mathbb{R}$，$\mathbf{x} \in \mathbb{R}^n$：

$$
\frac{\partial f}{\partial \mathbf{x}} = \nabla f = \begin{bmatrix} \frac{\partial f}{\partial x_1} \\ \vdots \\ \frac{\partial f}{\partial x_n} \end{bmatrix} \in \mathbb{R}^n
$$

### 向量对向量求导

设 $\mathbf{f}: \mathbb{R}^n \to \mathbb{R}^m$：

$$
\frac{\partial \mathbf{f}}{\partial \mathbf{x}} = \mathbf{J} = \begin{bmatrix}
\frac{\partial f_1}{\partial x_1} & \cdots & \frac{\partial f_1}{\partial x_n} \\
\vdots & \ddots & \vdots \\
\frac{\partial f_m}{\partial x_1} & \cdots & \frac{\partial f_m}{\partial x_n}
\end{bmatrix} \in \mathbb{R}^{m \times n}
$$

### 标量对矩阵求导

设 $f: \mathbb{R}^{m \times n} \to \mathbb{R}$，$\mathbf{X} \in \mathbb{R}^{m \times n}$：

$$
\frac{\partial f}{\partial \mathbf{X}} = \begin{bmatrix} 
\frac{\partial f}{\partial X_{11}} & \cdots & \frac{\partial f}{\partial X_{1n}} \\
\vdots & \ddots & \vdots \\
\frac{\partial f}{\partial X_{m1}} & \cdots & \frac{\partial f}{\partial X_{mn}}
\end{bmatrix} \in \mathbb{R}^{m \times n}
$$

```python
import numpy as np

def gradient_scalar_to_vector(f, x, h=1e-5):
    """标量对向量的梯度"""
    grad = np.zeros_like(x)
    for i in range(len(x)):
        x_plus = x.copy()
        x_minus = x.copy()
        x_plus[i] += h
        x_minus[i] -= h
        grad[i] = (f(x_plus) - f(x_minus)) / (2*h)
    return grad

def jacobian_vector_to_vector(f, x, h=1e-5):
    """向量对向量的雅可比矩阵"""
    y = f(x)
    m, n = len(y), len(x)
    J = np.zeros((m, n))
    
    for j in range(n):
        x_plus = x.copy()
        x_minus = x.copy()
        x_plus[j] += h
        x_minus[j] -= h
        J[:, j] = (f(x_plus) - f(x_minus)) / (2*h)
    
    return J

# 示例
# f(x) = ||x||^2 = sum(x_i^2)
f_scalar = lambda x: np.sum(x**2)
x = np.array([1.0, 2.0, 3.0])
grad = gradient_scalar_to_vector(f_scalar, x)
print(f"∇(||x||²) = {grad}")  # 应该是 [2, 4, 6]

# f(x) = [x_1 + x_2, x_1^2, x_2^3]
f_vector = lambda x: np.array([x[0] + x[1], x[0]**2, x[1]**3])
J = jacobian_vector_to_vector(f_vector, np.array([1.0, 2.0]))
print(f"雅可比矩阵:\n{J}")
# [[1, 1], [2, 0], [0, 12]]
```

---

## 雅可比矩阵

### 定义

向量值函数 $\mathbf{f}: \mathbb{R}^n \to \mathbb{R}^m$ 的**雅可比矩阵**：

$$
\mathbf{J} = \frac{\partial \mathbf{f}}{\partial \mathbf{x}} = \begin{bmatrix}
\frac{\partial f_1}{\partial x_1} & \cdots & \frac{\partial f_1}{\partial x_n} \\
\vdots & \ddots & \vdots \\
\frac{\partial f_m}{\partial x_1} & \cdots & \frac{\partial f_m}{\partial x_n}
\end{bmatrix}_{m \times n}
$$

### 链式法则的矩阵形式

对于 $\mathbf{z} = \mathbf{f}(\mathbf{y})$，$\mathbf{y} = \mathbf{g}(\mathbf{x})$：

$$
\frac{\partial \mathbf{z}}{\partial \mathbf{x}} = \frac{\partial \mathbf{z}}{\partial \mathbf{y}} \cdot \frac{\partial \mathbf{y}}{\partial \mathbf{x}} = \mathbf{J}_f \cdot \mathbf{J}_g
$$

### 常见函数的雅可比矩阵

**线性变换**：$\mathbf{f}(\mathbf{x}) = \mathbf{A}\mathbf{x}$

$$
\mathbf{J} = \mathbf{A}
$$

**逐元素函数**：$\mathbf{f}(\mathbf{x}) = [\sigma(x_1), \ldots, \sigma(x_n)]^\top$

$$
\mathbf{J} = \text{diag}(\sigma'(x_1), \ldots, \sigma'(x_n))
$$

**Softmax**：$f_i(\mathbf{x}) = \frac{e^{x_i}}{\sum_j e^{x_j}}$

$$
\frac{\partial f_i}{\partial x_j} = f_i(\delta_{ij} - f_j)
$$

$$
\mathbf{J} = \text{diag}(\mathbf{f}) - \mathbf{f}\mathbf{f}^\top
$$

```python
import numpy as np

def softmax(x):
    """数值稳定的 softmax"""
    x_max = np.max(x)
    exp_x = np.exp(x - x_max)
    return exp_x / np.sum(exp_x)

def softmax_jacobian(x):
    """Softmax 的雅可比矩阵"""
    s = softmax(x)
    return np.diag(s) - np.outer(s, s)

# 示例
x = np.array([1.0, 2.0, 3.0])
s = softmax(x)
J = softmax_jacobian(x)

print(f"Softmax(x) = {s}")
print(f"Softmax 雅可比矩阵:\n{J}")

# 验证：雅可比矩阵的每行之和为0
print(f"每行之和: {J.sum(axis=1)}")  # 接近 [0, 0, 0]
```

---

## 矩阵导数公式

### 基本公式

**线性函数**：$f(\mathbf{x}) = \mathbf{a}^\top \mathbf{x}$

$$
\frac{\partial f}{\partial \mathbf{x}} = \mathbf{a}
$$

**二次型**：$f(\mathbf{x}) = \mathbf{x}^\top \mathbf{A} \mathbf{x}$

$$
\frac{\partial f}{\partial \mathbf{x}} = (\mathbf{A} + \mathbf{A}^\top) \mathbf{x}
$$

若 $\mathbf{A}$ 对称：

$$
\frac{\partial f}{\partial \mathbf{x}} = 2\mathbf{A}\mathbf{x}
$$

### 矩阵导数

**矩阵乘法的迹**：$f(\mathbf{X}) = \text{tr}(\mathbf{A}\mathbf{X})$

$$
\frac{\partial f}{\partial \mathbf{X}} = \mathbf{A}^\top
$$

**二次型的迹**：$f(\mathbf{X}) = \text{tr}(\mathbf{X}^\top \mathbf{A} \mathbf{X})$

$$
\frac{\partial f}{\partial \mathbf{X}} = (\mathbf{A} + \mathbf{A}^\top)\mathbf{X}
$$

**行列式**：$f(\mathbf{X}) = \det(\mathbf{X})$

$$
\frac{\partial f}{\partial \mathbf{X}} = \det(\mathbf{X}) \cdot (\mathbf{X}^{-1})^\top
$$

**矩阵乘法**：$f(\mathbf{X}) = \mathbf{a}^\top \mathbf{X} \mathbf{b}$

$$
\frac{\partial f}{\partial \mathbf{X}} = \mathbf{a} \mathbf{b}^\top
$$

### 深度学习中常用的矩阵导数

**线性层**：$\mathbf{Y} = \mathbf{X}\mathbf{W}$，已知 $\frac{\partial L}{\partial \mathbf{Y}}$

$$
\frac{\partial L}{\partial \mathbf{W}} = \mathbf{X}^\top \frac{\partial L}{\partial \mathbf{Y}}
$$

$$
\frac{\partial L}{\partial \mathbf{X}} = \frac{\partial L}{\partial \mathbf{Y}} \mathbf{W}^\top
$$

```python
import numpy as np

def verify_matrix_derivatives():
    """验证矩阵导数公式"""
    
    # 1. 验证 tr(AX) 对 X 的导数 = A^T
    np.random.seed(42)
    A = np.random.randn(3, 4)
    X = np.random.randn(4, 3)
    
    def f1(X):
        return np.trace(A @ X)
    
    # 数值梯度
    h = 1e-5
    grad_numeric = np.zeros_like(X)
    for i in range(X.shape[0]):
        for j in range(X.shape[1]):
            X_plus = X.copy()
            X_minus = X.copy()
            X_plus[i, j] += h
            X_minus[i, j] -= h
            grad_numeric[i, j] = (f1(X_plus) - f1(X_minus)) / (2*h)
    
    grad_analytic = A.T
    print(f"tr(AX) 对 X 的导数:")
    print(f"  数值梯度范数误差: {np.linalg.norm(grad_numeric - grad_analytic):.2e}")
    
    # 2. 验证 a^T X b 对 X 的导数 = a b^T
    a = np.random.randn(3)
    b = np.random.randn(4)
    X = np.random.randn(3, 4)
    
    def f2(X):
        return a @ X @ b
    
    grad_numeric = np.zeros_like(X)
    for i in range(X.shape[0]):
        for j in range(X.shape[1]):
            X_plus = X.copy()
            X_minus = X.copy()
            X_plus[i, j] += h
            X_minus[i, j] -= h
            grad_numeric[i, j] = (f2(X_plus) - f2(X_minus)) / (2*h)
    
    grad_analytic = np.outer(a, b)
    print(f"\na^T X b 对 X 的导数:")
    print(f"  数值梯度范数误差: {np.linalg.norm(grad_numeric - grad_analytic):.2e}")

verify_matrix_derivatives()
```

---

## 深度学习中的梯度计算

### 线性层的梯度

**前向传播**：

$$
\mathbf{Y} = \mathbf{X}\mathbf{W} + \mathbf{b}
$$

其中：
- $\mathbf{X} \in \mathbb{R}^{B \times D_{in}}$：输入
- $\mathbf{W} \in \mathbb{R}^{D_{in} \times D_{out}}$：权重
- $\mathbf{b} \in \mathbb{R}^{D_{out}}$：偏置
- $\mathbf{Y} \in \mathbb{R}^{B \times D_{out}}$：输出

**反向传播**（已知 $\frac{\partial L}{\partial \mathbf{Y}}$）：

$$
\frac{\partial L}{\partial \mathbf{W}} = \mathbf{X}^\top \frac{\partial L}{\partial \mathbf{Y}}
$$

$$
\frac{\partial L}{\partial \mathbf{X}} = \frac{\partial L}{\partial \mathbf{Y}} \mathbf{W}^\top
$$

$$
\frac{\partial L}{\partial \mathbf{b}} = \sum_{batch} \frac{\partial L}{\partial \mathbf{Y}}
$$

```python
class LinearLayer:
    """线性层实现"""
    
    def __init__(self, in_features, out_features):
        self.W = np.random.randn(in_features, out_features) * 0.01
        self.b = np.zeros(out_features)
        
        # 缓存用于反向传播
        self.x = None
        
        # 梯度
        self.dW = None
        self.db = None
    
    def forward(self, x):
        """前向传播: Y = XW + b"""
        self.x = x  # 缓存输入
        return x @ self.W + self.b
    
    def backward(self, dL_dY):
        """反向传播"""
        # 对 W 的梯度: X^T @ dL/dY
        self.dW = self.x.T @ dL_dY
        
        # 对 b 的梯度: sum over batch
        self.db = np.sum(dL_dY, axis=0)
        
        # 对 X 的梯度: dL/dY @ W^T
        dL_dX = dL_dY @ self.W.T
        
        return dL_dX

# 测试
linear = LinearLayer(10, 5)
x = np.random.randn(32, 10)  # batch of 32

# 前向
y = linear.forward(x)
print(f"输出形状: {y.shape}")  # (32, 5)

# 反向
dL_dY = np.random.randn(32, 5)
dL_dX = linear.backward(dL_dY)
print(f"dW 形状: {linear.dW.shape}")  # (10, 5)
print(f"db 形状: {linear.db.shape}")  # (5,)
print(f"dL/dX 形状: {dL_dX.shape}")   # (32, 10)
```

### 激活函数的梯度

**Sigmoid**：$\sigma(x) = \frac{1}{1+e^{-x}}$

$$
\sigma'(x) = \sigma(x)(1 - \sigma(x))
$$

```python
def sigmoid(x):
    return 1 / (1 + np.exp(-np.clip(x, -500, 500)))

def sigmoid_backward(dL_dY, Y):
    """Sigmoid 反向传播"""
    # Y = sigmoid(x), dL/dx = dL/dY * sigmoid'(x) = dL/dY * Y * (1-Y)
    return dL_dY * Y * (1 - Y)
```

**ReLU**：$\text{ReLU}(x) = \max(0, x)$

$$
\text{ReLU}'(x) = \begin{cases} 1 & x > 0 \\ 0 & x \leq 0 \end{cases}
$$

```python
def relu(x):
    return np.maximum(0, x)

def relu_backward(dL_dY, X):
    """ReLU 反向传播"""
    return dL_dY * (X > 0).astype(float)
```

**Softmax + 交叉熵组合**

单独计算 Softmax 的梯度很复杂，但与交叉熵组合后非常简洁：

$$
\frac{\partial L}{\partial z_i} = \hat{y}_i - y_i
$$

**Softmax + 交叉熵梯度的推导**：

设 Softmax 输出为 $\hat{y}_i = \frac{e^{z_i}}{\sum_k e^{z_k}}$，交叉熵损失为 $L = -\sum_j y_j \log \hat{y}_j$。

**第一步**：计算 $\frac{\partial \hat{y}_j}{\partial z_i}$（Softmax 的雅可比矩阵）。

**当 $i = j$**：

$$\frac{\partial \hat{y}_i}{\partial z_i} = \frac{e^{z_i} \sum_k e^{z_k} - e^{z_i} \cdot e^{z_i}}{(\sum_k e^{z_k})^2} = \hat{y}_i(1 - \hat{y}_i)$$

**当 $i \neq j$**：

$$\frac{\partial \hat{y}_j}{\partial z_i} = \frac{0 - e^{z_j} \cdot e^{z_i}}{(\sum_k e^{z_k})^2} = -\hat{y}_j \hat{y}_i$$

综合：

$$\frac{\partial \hat{y}_j}{\partial z_i} = \hat{y}_j(\delta_{ij} - \hat{y}_i)$$

其中 $\delta_{ij}$ 是 Kronecker delta（$i=j$ 时为 1，否则为 0）。

**第二步**：使用链式法则计算 $\frac{\partial L}{\partial z_i}$。

$$\frac{\partial L}{\partial z_i} = \sum_j \frac{\partial L}{\partial \hat{y}_j} \cdot \frac{\partial \hat{y}_j}{\partial z_i}$$

其中：

$$\frac{\partial L}{\partial \hat{y}_j} = -\frac{y_j}{\hat{y}_j}$$

**第三步**：代入计算。

$$\frac{\partial L}{\partial z_i} = \sum_j \left(-\frac{y_j}{\hat{y}_j}\right) \cdot \hat{y}_j(\delta_{ij} - \hat{y}_i)$$

$$= -\sum_j y_j(\delta_{ij} - \hat{y}_i)$$

$$= -\sum_j y_j \delta_{ij} + \sum_j y_j \hat{y}_i$$

**第四步**：简化。

第一项：$\sum_j y_j \delta_{ij} = y_i$（只有 $j=i$ 时贡献非零）

第二项：$\sum_j y_j \hat{y}_i = \hat{y}_i \sum_j y_j = \hat{y}_i \cdot 1 = \hat{y}_i$（因为 $y$ 是 one-hot，和为 1）

因此：

$$\frac{\partial L}{\partial z_i} = -y_i + \hat{y}_i = \hat{y}_i - y_i$$

$$\boxed{\frac{\partial L}{\partial \mathbf{z}} = \hat{\mathbf{y}} - \mathbf{y}}$$

**直观理解**：
- $\hat{y}_i$ 是模型预测的概率
- $y_i$ 是真实标签（one-hot）
- 梯度 = 预测误差，非常简洁！

```python
def softmax(x):
    """数值稳定的 softmax"""
    x_max = np.max(x, axis=-1, keepdims=True)
    exp_x = np.exp(x - x_max)
    return exp_x / np.sum(exp_x, axis=-1, keepdims=True)

def cross_entropy_loss(predictions, targets):
    """
    交叉熵损失
    predictions: softmax 输出 (batch, num_classes)
    targets: one-hot 编码 (batch, num_classes)
    """
    epsilon = 1e-15
    predictions = np.clip(predictions, epsilon, 1 - epsilon)
    return -np.mean(np.sum(targets * np.log(predictions), axis=1))

def softmax_cross_entropy_backward(predictions, targets):
    """
    Softmax + 交叉熵的组合梯度
    
    组合后的梯度非常简洁: dL/dz = predictions - targets
    """
    return (predictions - targets) / predictions.shape[0]

# 示例
batch_size, num_classes = 4, 10
logits = np.random.randn(batch_size, num_classes)
targets_onehot = np.zeros((batch_size, num_classes))
targets_onehot[np.arange(batch_size), np.random.randint(0, num_classes, batch_size)] = 1

# 前向
probs = softmax(logits)
loss = cross_entropy_loss(probs, targets_onehot)

# 反向
grad = softmax_cross_entropy_backward(probs, targets_onehot)

print(f"损失: {loss:.4f}")
print(f"梯度形状: {grad.shape}")
```

### 损失函数的梯度

**MSE 损失**：$L = \frac{1}{n}\sum_i (y_i - \hat{y}_i)^2$

$$
\frac{\partial L}{\partial \hat{y}_i} = \frac{2}{n}(\hat{y}_i - y_i)
$$

```python
def mse_loss(predictions, targets):
    return np.mean((predictions - targets) ** 2)

def mse_loss_backward(predictions, targets):
    return 2 * (predictions - targets) / predictions.size
```

**交叉熵损失**：$L = -\sum_i y_i \log \hat{y}_i$

$$
\frac{\partial L}{\partial \hat{y}_i} = -\frac{y_i}{\hat{y}_i}
$$

---

## 反向传播算法

### 计算图

反向传播本质上是沿着**计算图**反向应用链式法则。

```
输入 x
    ↓
  线性层1 (W1, b1)
    ↓
  激活函数1
    ↓
  线性层2 (W2, b2)
    ↓
  激活函数2
    ↓
  损失函数
```

### 反向传播步骤

1. **前向传播**：计算并缓存中间结果
2. **计算损失**：得到标量损失值
3. **反向传播**：从损失开始，逐层计算梯度
4. **更新参数**：使用梯度更新权重

### 完整实现

```python
import numpy as np

class NeuralNetwork:
    """简单的前馈神经网络"""
    
    def __init__(self, layer_sizes):
        """
        layer_sizes: 各层大小列表，如 [784, 256, 128, 10]
        """
        self.weights = []
        self.biases = []
        
        # Xavier 初始化
        for i in range(len(layer_sizes) - 1):
            w = np.random.randn(layer_sizes[i], layer_sizes[i+1]) * \
                np.sqrt(2.0 / (layer_sizes[i] + layer_sizes[i+1]))
            b = np.zeros((1, layer_sizes[i+1]))
            self.weights.append(w)
            self.biases.append(b)
        
        # 缓存用于反向传播
        self.activations = []
        self.z_values = []
    
    def relu(self, z):
        return np.maximum(0, z)
    
    def relu_derivative(self, z):
        return (z > 0).astype(float)
    
    def softmax(self, z):
        z_max = np.max(z, axis=1, keepdims=True)
        exp_z = np.exp(z - z_max)
        return exp_z / np.sum(exp_z, axis=1, keepdims=True)
    
    def forward(self, x):
        """前向传播"""
        self.activations = [x]
        self.z_values = []
        
        current = x
        for i in range(len(self.weights)):
            z = current @ self.weights[i] + self.biases[i]
            self.z_values.append(z)
            
            # 最后一层用 softmax，其他用 ReLU
            if i == len(self.weights) - 1:
                current = self.softmax(z)
            else:
                current = self.relu(z)
            
            self.activations.append(current)
        
        return current
    
    def compute_loss(self, predictions, targets):
        """交叉熵损失"""
        epsilon = 1e-15
        predictions = np.clip(predictions, epsilon, 1 - epsilon)
        return -np.mean(np.sum(targets * np.log(predictions), axis=1))
    
    def backward(self, targets):
        """反向传播"""
        num_layers = len(self.weights)
        batch_size = targets.shape[0]
        
        # 初始化梯度存储
        dW = [None] * num_layers
        db = [None] * num_layers
        
        # 输出层梯度（softmax + 交叉熵组合）
        delta = (self.activations[-1] - targets) / batch_size
        
        # 从后向前传播梯度
        for i in range(num_layers - 1, -1, -1):
            # 计算对权重和偏置的梯度
            dW[i] = self.activations[i].T @ delta
            db[i] = np.sum(delta, axis=0, keepdims=True)
            
            if i > 0:
                # 传播到前一层
                delta = (delta @ self.weights[i].T) * self.relu_derivative(self.z_values[i-1])
        
        return dW, db
    
    def update_parameters(self, dW, db, learning_rate):
        """更新参数"""
        for i in range(len(self.weights)):
            self.weights[i] -= learning_rate * dW[i]
            self.biases[i] -= learning_rate * db[i]
    
    def train_step(self, x, targets, learning_rate):
        """单步训练"""
        # 前向传播
        predictions = self.forward(x)
        
        # 计算损失
        loss = self.compute_loss(predictions, targets)
        
        # 反向传播
        dW, db = self.backward(targets)
        
        # 更新参数
        self.update_parameters(dW, db, learning_rate)
        
        return loss

# 训练示例
np.random.seed(42)

# 创建网络
nn = NeuralNetwork([784, 256, 128, 10])

# 模拟数据
batch_size = 32
x = np.random.randn(batch_size, 784)
y = np.random.randint(0, 10, batch_size)
targets = np.zeros((batch_size, 10))
targets[np.arange(batch_size), y] = 1

# 训练
for epoch in range(10):
    loss = nn.train_step(x, targets, learning_rate=0.01)
    print(f"Epoch {epoch+1}, Loss: {loss:.4f}")
```

---

## 梯度数值验证

### 重要性

在实现反向传播时，很容易出错。数值梯度验证是确保正确性的关键步骤。

### 数值梯度

使用中心差分：

$$
\frac{\partial f}{\partial x_i} \approx \frac{f(x_i + h) - f(x_i - h)}{2h}
$$

### 验证函数

```python
import numpy as np

def numerical_gradient(f, x, h=1e-5):
    """
    计算数值梯度
    
    参数:
        f: 标量函数
        x: 参数（可以是向量、矩阵或任意形状）
        h: 差分步长
    
    返回:
        与 x 形状相同的梯度
    """
    grad = np.zeros_like(x)
    it = np.nditer(x, flags=['multi_index'], op_flags=['readwrite'])
    
    while not it.finished:
        idx = it.multi_index
        old_value = x[idx]
        
        x[idx] = old_value + h
        fx_plus = f()
        
        x[idx] = old_value - h
        fx_minus = f()
        
        grad[idx] = (fx_plus - fx_minus) / (2 * h)
        x[idx] = old_value
        
        it.iternext()
    
    return grad

def check_gradient(analytic_grad, numeric_grad, name="gradient", threshold=1e-6):
    """
    比较解析梯度和数值梯度
    """
    diff = np.abs(analytic_grad - numeric_grad)
    rel_error = diff / (np.abs(analytic_grad) + np.abs(numeric_grad) + 1e-8)
    max_error = np.max(rel_error)
    
    status = "✓ 通过" if max_error < threshold else "✗ 失败"
    print(f"{name}: 最大相对误差 = {max_error:.2e} {status}")
    
    return max_error < threshold

# 示例：验证神经网络的梯度
def verify_neural_network_gradients():
    np.random.seed(42)
    
    # 小网络便于验证
    nn = NeuralNetwork([4, 3, 2])
    
    # 小批量数据
    x = np.random.randn(2, 4)
    y = np.array([0, 1])
    targets = np.zeros((2, 2))
    targets[np.arange(2), y] = 1
    
    # 前向传播
    predictions = nn.forward(x)
    
    # 解析梯度
    dW, db = nn.backward(targets)
    
    # 数值验证每个权重矩阵
    for layer_idx in range(len(nn.weights)):
        # 验证 W
        def loss_fn_W():
            preds = nn.forward(x)
            return nn.compute_loss(preds, targets)
        
        numeric_dW = numerical_gradient(loss_fn_W, nn.weights[layer_idx].copy())
        check_gradient(dW[layer_idx], numeric_dW, f"W{layer_idx+1}")
        
        # 验证 b
        def loss_fn_b():
            preds = nn.forward(x)
            return nn.compute_loss(preds, targets)
        
        numeric_db = numerical_gradient(loss_fn_b, nn.biases[layer_idx].copy())
        check_gradient(db[layer_idx], numeric_db, f"b{layer_idx+1}")

verify_neural_network_gradients()
```

---

## 在深度学习中的应用

### 1. 自动微分系统

深度学习框架的核心是自动微分（autograd），它自动计算梯度：

```python
import numpy as np

class Tensor:
    """简化的自动微分张量"""
    
    def __init__(self, data, requires_grad=False, _children=(), _op=''):
        self.data = np.array(data)
        self.requires_grad = requires_grad
        self.grad = None
        self._backward = lambda: None
        self._prev = set(_children)
        self._op = _op
    
    def __add__(self, other):
        other = other if isinstance(other, Tensor) else Tensor(other)
        out = Tensor(self.data + other.data, _children=(self, other), _op='+')
        
        def _backward():
            if self.requires_grad:
                self.grad = self.grad + out.grad if self.grad else out.grad
            if other.requires_grad:
                other.grad = other.grad + out.grad if other.grad else out.grad
        out._backward = _backward
        return out
    
    def __mul__(self, other):
        other = other if isinstance(other, Tensor) else Tensor(other)
        out = Tensor(self.data * other.data, _children=(self, other), _op='*')
        
        def _backward():
            if self.requires_grad:
                self.grad = self.grad + other.data * out.grad if self.grad else other.data * out.grad
            if other.requires_grad:
                other.grad = other.grad + self.data * out.grad if other.grad else self.data * out.grad
        out._backward = _backward
        return out
    
    def matmul(self, other):
        """矩阵乘法"""
        out = Tensor(self.data @ other.data, _children=(self, other), _op='@')
        
        def _backward():
            if self.requires_grad:
                self.grad = self.grad + out.grad @ other.data.T if self.grad else out.grad @ other.data.T
            if other.requires_grad:
                other.grad = other.grad + self.data.T @ out.grad if other.grad else self.data.T @ out.grad
        out._backward = _backward
        return out
    
    def backward(self):
        """反向传播"""
        # 构建计算图
        topo = []
        visited = set()
        
        def build_topo(v):
            if v not in visited:
                visited.add(v)
                for child in v._prev:
                    build_topo(child)
                topo.append(v)
        
        build_topo(self)
        
        # 反向传播
        self.grad = np.ones_like(self.data)
        for v in reversed(topo):
            v._backward()

# 使用示例
x = Tensor([[1, 2], [3, 4]], requires_grad=True)
w = Tensor([[0.5, 0.5], [0.5, 0.5]], requires_grad=True)
y = x.matmul(w)
loss = y.mul(y)  # loss = (X @ W)^2
loss.backward()

print(f"x.grad:\n{x.grad}")
print(f"w.grad:\n{w.grad}")
```

### 2. Transformer 的梯度流

```python
def attention_gradients_demo():
    """演示注意力机制的梯度流"""
    np.random.seed(42)
    
    # 参数
    batch_size, seq_len, d_k = 2, 4, 8
    
    # 输入
    Q = np.random.randn(batch_size, seq_len, d_k)
    K = np.random.randn(batch_size, seq_len, d_k)
    V = np.random.randn(batch_size, seq_len, d_k)
    
    # 前向传播
    scores = Q @ K.transpose(0, 2, 1) / np.sqrt(d_k)
    
    # Softmax
    scores_max = scores.max(axis=-1, keepdims=True)
    exp_scores = np.exp(scores - scores_max)
    attention_weights = exp_scores / exp_scores.sum(axis=-1, keepdims=True)
    
    # 输出
    output = attention_weights @ V
    
    # 假设来自后续层的梯度
    d_output = np.random.randn(*output.shape)
    
    # 反向传播
    # dV
    dV = attention_weights.transpose(0, 2, 1) @ d_output
    
    # d_attention_weights
    d_attention_weights = d_output @ V.transpose(0, 2, 1)
    
    # d_scores (Softmax 反向)
    # ds = da * s - s * (da * s).sum()
    ds = d_attention_weights * attention_weights - attention_weights * (d_attention_weights * attention_weights).sum(axis=-1, keepdims=True)
    
    # dQ, dK
    dQ = ds @ K / np.sqrt(d_k)
    dK = ds.transpose(0, 2, 1) @ Q / np.sqrt(d_k)
    
    print(f"Attention 输出形状: {output.shape}")
    print(f"dQ 形状: {dQ.shape}")
    print(f"dK 形状: {dK.shape}")
    print(f"dV 形状: {dV.shape}")
    
    return dQ, dK, dV

dQ, dK, dV = attention_gradients_demo()
```

### 3. 梯度检查点（Gradient Checkpointing）

```python
def gradient_checkpointing_demo():
    """演示梯度检查点技术"""
    
    class CheckpointedFunction:
        """简化的梯度检查点实现"""
        
        def __init__(self, fn, *args):
            self.fn = fn
            self.args = args
            self.saved_tensors = None
        
        def forward(self):
            # 不保存中间激活，只保存输入
            self.saved_tensors = [arg.copy() for arg in self.args]
            return self.fn(*self.args)
        
        def backward(self, grad_output):
            # 重新计算前向传播以获取中间结果
            with np.enable_grad():  # 伪代码，实际框架会追踪梯度
                output = self.fn(*self.saved_tensors)
                # 计算梯度...
            return grad_output
    
    print("梯度检查点通过牺牲计算时间换取内存")
    print("常用于训练大型 Transformer 模型")
```

### 4. 混合精度训练的梯度处理

```python
def mixed_precision_gradients():
    """混合精度训练中的梯度缩放"""
    
    # 模拟 FP16 前向传播
    def forward_fp16(x, w):
        x_fp16 = x.astype(np.float16)
        w_fp16 = w.astype(np.float16)
        return (x_fp16 @ w_fp16).astype(np.float32)
    
    # 梯度缩放
    class GradScaler:
        def __init__(self, init_scale=2**16):
            self.scale = init_scale
        
        def scale_loss(self, loss):
            return loss * self.scale
        
        def unscale_grad(self, grad):
            return grad / self.scale
        
        def update(self, has_inf_or_nan):
            if has_inf_or_nan:
                self.scale *= 0.5
            else:
                self.scale *= 2.0
    
    scaler = GradScaler()
    print(f"初始缩放因子: {scaler.scale}")
    print("混合精度训练通过 FP16 加速计算，同时保持 FP32 精度")
```

### 5. ResNet 残差连接的梯度流

残差网络（ResNet）通过**跳跃连接**（Skip Connection）解决了深层网络的梯度消失问题。

#### 残差块结构

标准残差块的前向传播：

$$\mathbf{y} = \mathcal{F}(\mathbf{x}, \{W_i\}) + \mathbf{x}$$

其中：
- $\mathbf{x}$ 是输入
- $\mathcal{F}(\mathbf{x}, \{W_i\})$ 是残差映射（通常包含 2-3 个卷积层）
- $\mathbf{y}$ 是输出

#### 残差连接梯度的完整推导

**目标**：推导梯度 $\frac{\partial L}{\partial \mathbf{x}}$ 如何通过残差连接流动。

**第一步**：写出前向传播。

$$\mathbf{y} = \mathcal{F}(\mathbf{x}) + \mathbf{x}$$

**第二步**：应用链式法则。

$$\frac{\partial L}{\partial \mathbf{x}} = \frac{\partial L}{\partial \mathbf{y}} \cdot \frac{\partial \mathbf{y}}{\partial \mathbf{x}}$$

**第三步**：计算 $\frac{\partial \mathbf{y}}{\partial \mathbf{x}}$。

$$\frac{\partial \mathbf{y}}{\partial \mathbf{x}} = \frac{\partial}{\partial \mathbf{x}}(\mathcal{F}(\mathbf{x}) + \mathbf{x}) = \frac{\partial \mathcal{F}}{\partial \mathbf{x}} + \mathbf{I}$$

其中 $\mathbf{I}$ 是单位矩阵。

**第四步**：得到最终梯度公式。

$$\frac{\partial L}{\partial \mathbf{x}} = \frac{\partial L}{\partial \mathbf{y}} \left(\frac{\partial \mathcal{F}}{\partial \mathbf{x}} + \mathbf{I}\right)$$

$$\boxed{\frac{\partial L}{\partial \mathbf{x}} = \frac{\partial L}{\partial \mathbf{y}} \cdot \frac{\partial \mathcal{F}}{\partial \mathbf{x}} + \frac{\partial L}{\partial \mathbf{y}}}$$

#### 深层网络的梯度传播

对于 $L$ 层的残差网络，设第 $l$ 层的输出为 $\mathbf{x}_{l+1} = \mathcal{F}_l(\mathbf{x}_l) + \mathbf{x}_l$。

**从第 $L$ 层到第 $l$ 层的梯度**：

$$\frac{\partial L}{\partial \mathbf{x}_l} = \frac{\partial L}{\partial \mathbf{x}_L} \prod_{i=l}^{L-1} \left(\frac{\partial \mathcal{F}_i}{\partial \mathbf{x}_i} + \mathbf{I}\right)$$

**关键观察**：
- 即使 $\frac{\partial \mathcal{F}_i}{\partial \mathbf{x}_i}$ 很小（梯度消失），$\mathbf{I}$ 项保证至少有 $\frac{\partial L}{\partial \mathbf{x}_L}$ 能直接传递
- 这相当于为梯度提供了一条"高速公路"

#### 与普通网络的对比

**普通深层网络**：

$$\frac{\partial L}{\partial \mathbf{x}_l} = \frac{\partial L}{\partial \mathbf{x}_L} \prod_{i=l}^{L-1} \frac{\partial \mathcal{F}_i}{\partial \mathbf{x}_i}$$

当网络很深时，如果 $\|\frac{\partial \mathcal{F}_i}{\partial \mathbf{x}_i}\| < 1$，梯度会指数级衰减。

**残差网络**：

$$\frac{\partial L}{\partial \mathbf{x}_l} = \frac{\partial L}{\partial \mathbf{x}_L} \prod_{i=l}^{L-1} \left(\mathbf{I} + \frac{\partial \mathcal{F}_i}{\partial \mathbf{x}_i}\right)$$

由于 $\mathbf{I}$ 项的存在，即使某些 $\mathcal{F}_i$ 的梯度很小，整体梯度仍能有效传播。

#### 恒等映射的条件

He et al. (2016) 证明了当残差块满足"恒等映射"条件时，梯度传播最优：

$$\mathbf{x}_{l+1} = \mathbf{x}_l + \mathcal{F}_l(\mathbf{x}_l)$$

**推导**：假设 $\mathcal{F}_l(\mathbf{x}_l) \approx 0$（初始化接近零），则：

$$\mathbf{x}_{l+1} \approx \mathbf{x}_l$$

这意味着初始训练时信号可以直接传递，梯度也能顺畅回传。

#### 代码实现

```python
import numpy as np

class ResidualBlock:
    """残差块实现"""
    
    def __init__(self, in_channels, out_channels, stride=1):
        # 主路径：两个 3x3 卷积
        self.conv1 = np.random.randn(3, 3, in_channels, out_channels) * 0.01
        self.conv2 = np.random.randn(3, 3, out_channels, out_channels) * 0.01
        
        # 如果维度不匹配，需要 1x1 卷积调整
        self.shortcut_conv = None
        if stride != 1 or in_channels != out_channels:
            self.shortcut_conv = np.random.randn(1, 1, in_channels, out_channels) * 0.01
        
        self.cache = None
    
    def forward(self, x):
        """
        前向传播: y = F(x) + x (或 shortcut)
        """
        identity = x
        
        # 主路径 F(x)
        out = self._conv2d(x, self.conv1)
        out = self._relu(out)
        out = self._conv2d(out, self.conv2)
        
        # 跳跃连接
        if self.shortcut_conv is not None:
            identity = self._conv2d(x, self.shortcut_conv)
        
        # 残差连接: 这是关键！
        out = out + identity
        
        self.cache = (x, identity, out)
        return self._relu(out)
    
    def backward(self, grad_output):
        """
        反向传播演示梯度流
        
        关键：梯度通过两条路径回传
        dL/dx = dL/dy * (dF/dx + I)
        """
        x, identity, pre_relu = self.cache
        
        # ReLU 的梯度
        grad = grad_output * (pre_relu > 0)
        
        # 残差连接的梯度分流
        # 一条路径：通过主路径
        grad_main = self._conv2d_backward(grad, self.conv2)
        
        # 另一条路径：直接传递（跳跃连接）
        # 这就是"梯度高速公路"！
        grad_skip = grad if self.shortcut_conv is None else self._conv2d_backward(grad, self.shortcut_conv)
        
        # 合并两条路径的梯度
        grad_input = grad_main + grad_skip
        
        return grad_input
    
    def _conv2d(self, x, w):
        """简化的卷积操作"""
        return x  # 实际实现省略
    
    def _conv2d_backward(self, grad, w):
        """简化的卷积反向传播"""
        return grad  # 实际实现省略
    
    def _relu(self, x):
        return np.maximum(0, x)


def demonstrate_gradient_flow():
    """演示残差连接如何保持梯度流"""
    
    # 模拟深层网络
    num_layers = 100
    
    # 普通网络：梯度逐层衰减
    grad_normal = 1.0
    avg_grad_magnitude = 0.9  # 假设每层梯度缩小 10%
    
    for i in range(num_layers):
        grad_normal *= avg_grad_magnitude
    
    print(f"普通网络 {num_layers} 层后的梯度: {grad_normal:.6e}")
    
    # 残差网络：梯度保持
    grad_residual = 1.0
    
    for i in range(num_layers):
        # 即使 F 的梯度很小，I 项保证梯度不会消失
        # 假设 dF/dx ≈ 0.1，加上 I 项
        grad_residual *= (0.1 + 1.0)  # 1.1 > 1
    
    print(f"残差网络 {num_layers} 层后的梯度: {grad_residual:.6e}")
    
    print("\n关键结论：")
    print("1. 普通网络梯度指数衰减，深层几乎无梯度")
    print("2. 残差网络梯度有 '恒等映射' 保护，深层仍能有效学习")

demonstrate_gradient_flow()
```

#### 输出示例

```
普通网络 100 层后的梯度: 2.656140e-05
残差网络 100 层后的梯度: 1.378061e+04

关键结论：
1. 普通网络梯度指数衰减，深层几乎无梯度
2. 残差网络梯度有 '恒等映射' 保护，深层仍能有效学习
```

#### ResNet 变体的梯度特性

| 变体 | 跳跃连接方式 | 梯度特性 |
|------|-------------|----------|
| ResNet-v1 | $\mathbf{x} + \mathcal{F}(\mathbf{x})$ | 基础残差连接 |
| ResNet-v2 | Pre-activation | 更好的梯度流 |
| DenseNet | 密集连接 | 梯度从多层直达 |
| Wide ResNet | 加宽残差块 | 更多梯度路径 |

#### 数学证明：为什么残差网络更容易优化

**定理**：对于任意小的 $\epsilon > 0$，存在残差网络使得 $\|\frac{\partial L}{\partial \mathbf{x}_l} - \frac{\partial L}{\partial \mathbf{x}_L}\| < \epsilon$。

**证明思路**：

设 $\mathcal{F}_i(\mathbf{x}) \equiv 0$（恒等映射），则：

$$\mathbf{x}_{l+1} = \mathbf{x}_l + 0 = \mathbf{x}_l$$

$$\frac{\partial \mathbf{x}_{l+1}}{\partial \mathbf{x}_l} = \mathbf{I}$$

因此：

$$\frac{\partial L}{\partial \mathbf{x}_l} = \frac{\partial L}{\partial \mathbf{x}_L} \cdot \mathbf{I}^{L-l} = \frac{\partial L}{\partial \mathbf{x}_L}$$

这说明残差结构**至少**不会让梯度消失，是普通网络做不到的。

### 6. 实际框架对比

| 特性 | PyTorch | TensorFlow | JAX |
|------|---------|------------|-----|
| 自动微分 | torch.autograd | tf.GradientTape | jax.grad |
| 计算图 | 动态图 | 静态/动态 | 函数变换 |
| 梯度累积 | 支持 | 支持 | 支持 |
| 混合精度 | torch.cuda.amp | tf.mixed_precision | jax.default_matmul_precision |

---

## 小结

本章介绍了向量矩阵微积分和反向传播算法：

| 概念 | 公式/定义 | 应用 |
|------|----------|------|
| 雅可比矩阵 | $\mathbf{J}_{ij} = \frac{\partial f_i}{\partial x_j}$ | 向量值函数导数 |
| 线性层梯度 | $\frac{\partial L}{\partial \mathbf{W}} = \mathbf{X}^\top \frac{\partial L}{\partial \mathbf{Y}}$ | 反向传播 |
| Softmax 梯度 | $\frac{\partial f_i}{\partial x_j} = f_i(\delta_{ij} - f_j)$ | 分类输出 |
| Softmax+交叉熵 | $\frac{\partial L}{\partial z_i} = \hat{y}_i - y_i$ | 简洁的梯度 |

### 关键公式

**矩阵导数**：
- $\frac{\partial}{\partial \mathbf{X}} \text{tr}(\mathbf{A}\mathbf{X}) = \mathbf{A}^\top$
- $\frac{\partial}{\partial \mathbf{X}} \mathbf{a}^\top \mathbf{X} \mathbf{b} = \mathbf{a}\mathbf{b}^\top$
- $\frac{\partial}{\partial \mathbf{X}} \det(\mathbf{X}) = \det(\mathbf{X})(\mathbf{X}^{-1})^\top$

**反向传播核心**：
- 前向传播：缓存中间结果
- 反向传播：逐层应用链式法则
- 数值验证：确保梯度正确

---

**上一节**：[第二章（c）：高阶导数与泰勒展开](02c-高阶导数与泰勒展开.md)

**下一章**：[第三章：概率论](03-probability.md) - 学习概率分布、条件概率、贝叶斯定理等概念。

**返回**：[数学基础教程目录](../math-fundamentals.md)
